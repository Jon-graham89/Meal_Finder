//random Meals

const randomMealIngredientList = () =>{
    randomMealIngredients = [];
        if(randomMeal.strIngredient1 === ''){
            return null
        } else {
            randomMealIngredients.push(`${randomMeal.strIngredient1}: ${randomMeal.strMeasure1}`)
        };
        if(randomMeal.strIngredient2 === ''){
            return null
        } else {
            randomMealIngredients.push(`${randomMeal.strIngredient2}: ${randomMeal.strMeasure2}`)
        };
        if(randomMeal.strIngredient3 === ''){
            return null
        } else {
            randomMealIngredients.push(`${randomMeal.strIngredient3}: ${randomMeal.strMeasure3}`)
        };
        if(randomMeal.strIngredient4 === ''){
            return null
        } else {
            randomMealIngredients.push(`${randomMeal.strIngredient4}: ${randomMeal.strMeasure4}`)
        };
        if(randomMeal.strIngredient5 === ''){
            return null
        } else {
            randomMealIngredients.push(`${randomMeal.strIngredient5}: ${randomMeal.strMeasure5}`)
        };
        if(randomMeal.strIngredient6 === ''){
            return null
        } else {
            randomMealIngredients.push(`${randomMeal.strIngredient6}: ${randomMeal.strMeasure6}`)
        };
        if(randomMeal.strIngredient7 === ''){
            return null
        } else {
            randomMealIngredients.push(`${randomMeal.strIngredient7}: ${randomMeal.strMeasure7}`)
        };
        if(randomMeal.strIngredient8 === ''){
            return null
        } else {
            randomMealIngredients.push(`${randomMeal.strIngredient8}: ${randomMeal.strMeasure8}`)
        };
        if(randomMeal.strIngredient9 === ''){
            return null
        } else {
            randomMealIngredients.push(`${randomMeal.strIngredient9}: ${randomMeal.strMeasure9}`)
        };
        if(randomMeal.strIngredient10 === ''){
            return null
        } else {
            randomMealIngredients.push(`${randomMeal.strIngredient10}: ${randomMeal.strMeasure10}`)
        };
        if(randomMeal.strIngredient11 === ''){
            return null
        } else {
            randomMealIngredients.push(`${randomMeal.strIngredient11}: ${randomMeal.strMeasure11}`)
        };
        if(randomMeal.strIngredient12 === ''){
            return null
        } else {
            randomMealIngredients.push(`${randomMeal.strIngredient12}: ${randomMeal.strMeasure12}`)
        };
        if(randomMeal.strIngredient13 === ''){
            return null
        } else {
            randomMealIngredients.push(`${randomMeal.strIngredient13}: ${randomMeal.strMeasure13}`)
        };
        if(randomMeal.strIngredient14 === ''){
            return null
        } else {
            randomMealIngredients.push(`${randomMeal.strIngredient14}: ${randomMeal.strMeasure14}`)
        };
        if(randomMeal.strIngredient15 === ''){
            return null
        } else {
            randomMealIngredients.push(`${randomMeal.strIngredient15}: ${randomMeal.strMeasure15}`)
        };
        if(randomMeal.strIngredient16 === ''){
            return null
        } else {
            randomMealIngredients.push(`${randomMeal.strIngredient16}: ${randomMeal.strMeasure16}`)
        };
        if(randomMeal.strIngredient17 === ''){
            return null
        } else {
            randomMealIngredients.push(`${randomMeal.strIngredient17}: ${randomMeal.strMeasure17}`)
        };
        if(randomMeal.strIngredient18 === ''){
            return null
        } else {
            randomMealIngredients.push(`${randomMeal.strIngredient18}: ${randomMeal.strMeasure18}`)
        };
        if(randomMeal.strIngredient19 === ''){
            return null
        } else {
            randomMealIngredients.push(`${randomMeal.strIngredient19}: ${randomMeal.strMeasure19}`)
        };
        if(randomMeal.strIngredient20 === ''){
            return null
        } else {
            randomMealIngredients.push(`${randomMeal.strIngredient20}: ${randomMeal.strMeasure20}`)
        };
}




//searched meals

const ingredientList = () =>{
    ingredientsList = [];
    
       if(searchedMeals[i].strIngredient1 === ''){
           return null;
       } else {
           ingredientsList.push(`${searchedMeals[i].strIngredient1}: ${searchedMeals[i].strMeasure1}`)
       };
       if(searchedMeals[i].strIngredient2 === ''){
           return null;
       } else {
           ingredientsList.push(`${searchedMeals[i].strIngredient2}: ${searchedMeals[i].strMeasure2}`)
       };
       if(searchedMeals[i].strIngredient3 === ''){
           return null;
       } else {
           ingredientsList.push(`${searchedMeals[i].strIngredient3}: ${searchedMeals[i].strMeasure3}`)
       };
       if(searchedMeals[i].strIngredient4 === ''){
           return null;
       } else {
           ingredientsList.push(`${searchedMeals[i].strIngredient4}: ${searchedMeals[i].strMeasure4}`)
       };
       if(searchedMeals[i].strIngredient5 === ''){
           return null;
       } else {
           ingredientsList.push(`${searchedMeals[i].strIngredient5}: ${searchedMeals[i].strMeasure5}`)
       };
       if(searchedMeals[i].strIngredient6 === ''){
           return null;
       } else {
           ingredientsList.push(`${searchedMeals[i].strIngredient6}: ${searchedMeals[i].strMeasure6}`)
       };
       if(searchedMeals[i].strIngredient7 === ''){
           return null;
       } else {
           ingredientsList.push(`${searchedMeals[i].strIngredient7}: ${searchedMeals[i].strMeasure7}`)
       };
       if(searchedMeals[i].strIngredient8 === ''){
           return null;
       } else {
           ingredientsList.push(`${searchedMeals[i].strIngredient8}: ${searchedMeals[i].strMeasure8}`)
       };
       if(searchedMeals[i].strIngredient9 === ''){
           return null;
       } else {
           ingredientsList.push(`${searchedMeals[i].strIngredient9}: ${searchedMeals[i].strMeasure9}`)
       };
       if(searchedMeals[i].strIngredient10 === ''){
           return null;
       } else {
           ingredientsList.push(`${searchedMeals[i].strIngredient10}: ${searchedMeals[i].strMeasure10}`)
       };
       if(searchedMeals[i].strIngredient11 === ''){
           return null;
       } else {
           ingredientsList.push(`${searchedMeals[i].strIngredient11}: ${searchedMeals[i].strMeasure11}`)
       };
       if(searchedMeals[i].strIngredient12 === ''){
           return null;
       } else {
           ingredientsList.push(`${searchedMeals[i].strIngredient12}: ${searchedMeals[i].strMeasure12}`)
       };
       if(searchedMeals[i].strIngredient13 === ''){
           return null;
       } else {
           ingredientsList.push(`${searchedMeals[i].strIngredient13}: ${searchedMeals[i].strMeasure13}`)
       };
       if(searchedMeals[i].strIngredient14 === ''){
           return null;
       } else {
           ingredientsList.push(`${searchedMeals[i].strIngredient14}: ${searchedMeals[i].strMeasure14}`)
       };
       if(searchedMeals[i].strIngredient15 === ''){
           return null;
       } else {
           ingredientsList.push(`${searchedMeals[i].strIngredient15}: ${searchedMeals[i].strMeasure15}`)
       };
       if(searchedMeals[i].strIngredient16 === ''){
           return null;
       } else {
           ingredientsList.push(`${searchedMeals[i].strIngredient16}: ${searchedMeals[i].strMeasure16}`)
       };
       if(searchedMeals[i].strIngredient17 === ''){
           return null;
       } else {
           ingredientsList.push(`${searchedMeals[i].strIngredient17}: ${searchedMeals[i].strMeasure17}`)
       };
       if(searchedMeals[i].strIngredient18 === ''){
           return null;
       } else {
           ingredientsList.push(`${searchedMeals[i].strIngredient18}: ${searchedMeals[i].strMeasure18}`)
       };
       if(searchedMeals[i].strIngredient19 === ''){
           return null;
       } else {
           ingredientsList.push(`${searchedMeals[i].strIngredient19}: ${searchedMeals[i].strMeasure19}`)
       };
       if(searchedMeals[i].strIngredient20 !== ""){
           ingredientsList.push(`${searchedMeals[i].strIngredient20}: ${searchedMeals[i].strMeasure20}`)
       } else {
           return null;
       };

       
}